# 🛠️ Utility Tools

The `/tools` directory contains auxiliary scripts that support the application's infrastructure.

- **saver.py**: Manages file I/O operations. It includes logic to verify the existence of the `/data` directory and create it if necessary, ensuring that travel profiles are stored correctly regardless of the initial environment state.